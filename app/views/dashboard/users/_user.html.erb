<%= link_to edit_dashboard_user_path(user), class: 'bg-white w-full flex items-center group' do %>
	<div class="w-40 md:w-36">
		<div class="aspect-w-1 aspect-h-1 w-full relative leading-none overflow-hidden block bg-shale">
		</div>
	</div>

	<div class="w-full h-full p-4 flex flex-col justify-center">
		<h3 class="capitalize font-serif text-xl inline leading-none"><%= user.name %></h3>
		<div class="flex items-center">
			<p class="opacity-50 text-sm leading-none mt-1"><%= user.email %> · <%= user.phone %></p>
		</div>
	</div>
<% end %>