<%= simple_form_for @workout do |f| %>
	<%= f.input :name %>
	<%= f.input :intensity %>
	<%= f.input :mins %>
	<%= f.input :video_url %>
	<%= f.association :category %>
	<%= f.association :instructor %>
	<%= f.input :membership %>
	<%= f.submit %>
<% end %>